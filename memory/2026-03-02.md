# 2026-03-02

## Lumina v1.6 Status
- **Build Size:** 572.68 KB
- **Latest Commit:** `d842fcc` - fix: Widget de clima restaurado na Home
- **Stable Tag:** `v1.5.6-stable`

### Features Completed
- IP Camera system (independent, max 4 cameras, RTSP support)
- Favorites bar with reordering (up/down arrows), all device types
- Offline mode: Tailwind bundled, CSS gradient fallbacks for images
- Auto-Sync with Hub Variables (6 chunked variables)
- Auto-Setup Link (`?config=BASE64_JSON` URL param)
- Dashboard Tile drivers (`LuminaDashboardTile_PT/EN.groovy`)
- Installer with "Generate Access Link" and auto-setup variables
- Documentation: INSTALLATION.md (3 methods), bilingual READMEs
- Video script: `ROTEIRO_VIDEO_COMERCIAL.md` (75 seconds)
- Weather widget restored on Home screen

### Bug Under Investigation
- **Weather Widget Click:** Home tab widget doesn't open city config
- `showWeatherSettings` state exists, gear icon works at line 1384
- Home tab weather widget at line 1184 may lack onClick handler

### File Naming Convention
- English Groovy files use `*_EN.groovy` suffix
- Portuguese files use `*_PT.groovy` suffix

## LG TV Support (v1.6-beta) ✅ FIXED
- **Commits:** `81fc1eb` (fix), `3cb43a6` (clean)
- **Test Device:** "LG Bancada" - Device ID 419, Model 32LK615BPSB, webOS 4.0
- **Driver:** "LGTV with webOS" by Dan Danache (Codeberg)
- **Detection Logic:** `target.name.toLowerCase().includes('lg')` for LG vs Samsung
- **LG Commands:** `pushRemoteButtons` with button names (UP, DOWN, LEFT, RIGHT, ENTER, BACK, HOME)
- **Samsung Commands:** Direct commands (arrowUp, arrowDown, arrowLeft, arrowRight, enter, Return, home)
- **Bug Found & Fixed:** Remote components (LGRemote, ACRemote, DenonRemote) were using OLD object syntax for sendHubitatCommand instead of positional params
- **URL Format:** `/devices/{id}/{command}/{args}` → `/devices/419/pushRemoteButtons/UP`

## Camera Detection Fix
- TVs excluded from camera detection via capability check (`tv`, `samsungtv`, `audiovolume`)
- Also excludes by name: `tv`, `samsung`, `lg`, `webos`
- Location: `lumina-build/services/hubitatService.ts` line ~554

## Next Steps
1. Check browser console for `[Hubitat CMD]` logs when clicking TV buttons
2. Verify `isLG` variable correctly identifies "LG Bancada"
3. Fix `handleTVNavigation` if commands not firing
4. Port LG support to v1.5 after v1.6 validation
5. Test IP cameras (Mibo RTSP, Hikvision)
6. Fix weather widget click-to-configure on Home

## Technical Decisions
- Tailwind v4 requires `@tailwindcss/postcss` package
- HPM repo: `https://raw.githubusercontent.com/Domotika/LuminaDev/main/repository.json`
- License: Commercial, one per hub, activation via Installation ID
